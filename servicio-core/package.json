{
  "name": "servicio-core",
  "version": "1.0.4",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:reset": "prisma migrate reset --force",
    "test": "node --test",
    "test:integration": "node --test tests/estadoConversacionIntegracion.test.js"
  },
  "dependencies": {
    "@begabot/shared": "file:../packages/shared-contracts",
    "@prisma/client": "^6.2.0",
    "express": "^5.2.1",
    "prisma": "^6.2.0"
  }
}
